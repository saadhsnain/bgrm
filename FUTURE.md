# Future

- Add shared-canvas scaling for batches of icons.
- Add dark, light, and brand-color preview backgrounds.
- Add side-by-side before/after preview generation.
- Add a small GUI or drag-and-drop interface.
- Add a FastAPI endpoint if this needs to run as a service.
